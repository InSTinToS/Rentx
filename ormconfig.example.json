{
  "port": 5432,
  "type": "postgres",
  "host": "localhost",
  "username": "same as dockercompose env",
  "password": "same as dockercompose env",
  "database": "rentx",
  "migrations": ["**/migrations/*.ts"],
  "entities": ["**/models/*.ts"],
  "cli": {
    "migrationsDir": "./src/shared/infra/database/postgres/typeorm/migrations"
  }
}
